// Fixed injectDomainTags function with proper capitalization
function injectDomainTags(content: string, domains: string[]): string {
  if (!domains || domains.length === 0) {
    return content;
  }

  // Create properly formatted domain tags with capital first letters
  const domainTagsLine = domains
    .map(domain => {
      // Handle case where domain might already have @ prefix
      const cleanDomain = domain.startsWith('@') ? domain.slice(1) : domain;
      return `@${cleanDomain.charAt(0).toUpperCase() + cleanDomain.slice(1).toLowerCase()}`;
    })
    .join(' ');
  
  // Split content into lines for processing
  const lines = content.split('\n');
  
  // Find the first non-empty line
  let firstNonEmptyIndex = 0;
  while (firstNonEmptyIndex < lines.length && lines[firstNonEmptyIndex].trim() === '') {
    firstNonEmptyIndex++;
  }
  
  if (firstNonEmptyIndex >= lines.length) {
    return domainTagsLine;
  }
  
  const firstNonEmptyLine = lines[firstNonEmptyIndex].trim();
  
  // Case 1: Replace existing tags (including @Generic)
  if (firstNonEmptyLine.startsWith('@')) {
    lines[firstNonEmptyIndex] = domainTagsLine;
    return lines.join('\n');
  }
  
  // Case 2: Insert before Feature: line
  if (firstNonEmptyLine.startsWith('Feature:')) {
    lines.splice(firstNonEmptyIndex, 0, domainTagsLine, '');
    return lines.join('\n');
  }
  
  // Case 3: Prepend to beginning
  return [domainTagsLine, '', ...lines].join('\n');
}

// Updated generateFeature function (showing the relevant parts)
async function generateFeature(title: string, story: string) {
  try {
    // 1. First, infer the domains from title and story
    const domainInferencePrompt = `
      Based on the following feature title and story, determine which domain tags apply.
      Return ONLY a JSON array of domain names (lowercase, no @ symbols).
      
      Available domains: gdpr, security, performance, accessibility, analytics, payments, notifications, auth, api, ui, database, mobile, testing, deployment, monitoring, compliance, internationalization, seo, caching, backup, logging, email, integration, workflow, reporting, search, messaging, social, media, content, admin, billing, subscription, user-management, file-upload, validation, error-handling, configuration, documentation, audit, maintenance, scalability, optimization, migration, backup-recovery, disaster-recovery, load-balancing, microservices, containerization, kubernetes, docker, aws, azure, gcp, serverless, lambda, graphql, rest-api, websockets, real-time, streaming, batch-processing, machine-learning, ai, data-science, analytics-reporting, business-intelligence, data-warehouse, etl, data-pipeline, data-governance, data-privacy, data-retention, data-archiving, data-migration, data-backup, data-recovery, data-synchronization, data-validation, data-transformation, data-enrichment, data-cleansing, data-quality, data-lineage, data-catalog, data-discovery, data-profiling, data-masking, data-encryption, data-compression, data-deduplication, data-partitioning, data-sharding, data-replication, data-distribution, data-consistency, data-integrity, data-availability, data-durability, data-reliability, data-security, data-compliance, data-audit, data-monitoring, data-alerting, data-visualization, data-dashboards, data-reporting, data-exports, data-imports, data-feeds, data-apis, data-services, data-models, data-schemas, data-structures, data-formats, data-standards, data-protocols, data-interfaces, data-contracts, data-agreements, data-policies, data-procedures, data-guidelines, data-best-practices, data-patterns, data-anti-patterns, data-design-patterns, data-architecture-patterns, data-integration-patterns, data-processing-patterns, data-storage-patterns, data-access-patterns, data-security-patterns, data-privacy-patterns, data-governance-patterns, data-quality-patterns, data-lifecycle-patterns, data-migration-patterns, data-backup-patterns, data-recovery-patterns, data-disaster-recovery-patterns, data-business-continuity-patterns, data-performance-patterns, data-scalability-patterns, data-optimization-patterns, data-monitoring-patterns, data-alerting-patterns, data-logging-patterns, data-auditing-patterns, data-compliance-patterns, data-documentation-patterns, data-testing-patterns, data-validation-patterns, data-verification-patterns, data-certification-patterns, data-accreditation-patterns, data-standardization-patterns, data-harmonization-patterns, data-normalization-patterns, data-denormalization-patterns, data-aggregation-patterns, data-disaggregation-patterns, data-summarization-patterns, data-drill-down-patterns, data-roll-up-patterns, data-pivot-patterns, data-unpivot-patterns, data-transpose-patterns, data-reshape-patterns, data-melt-patterns, data-cast-patterns, data-join-patterns, data-union-patterns, data-intersect-patterns, data-except-patterns, data-merge-patterns, data-split-patterns, data-chunk-patterns, data-batch-patterns, data-stream-patterns, data-window-patterns, data-session-patterns, data-event-patterns, data-time-series-patterns, data-spatial-patterns, data-graph-patterns, data-network-patterns, data-tree-patterns, data-hierarchy-patterns, data-taxonomy-patterns, data-ontology-patterns, data-semantic-patterns, data-syntactic-patt